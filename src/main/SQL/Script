criação da tabela usuario
        create table usuario(
         id int not null auto_increment primary key,
         nome varchar(255),
         email varchar(255),
         senha varchar(23),
         cpf long,
         grupo varchar(255),
         ativo boolean
        )